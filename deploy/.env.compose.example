# For local Docker and EC2 Docker Compose deployments.
# BITOGUARD_SOURCE_URL defaults to the live BitoPro AWS Event API.
# Override it only if you want to point sync at a different source.

BITOGUARD_SOURCE_URL=https://aws-event-api.bitopro.com
BITOGUARD_LABEL_SOURCE=hidden_suspicious_label
